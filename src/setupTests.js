// Runs before every test file (configured in vite.config.js -> test.setupFiles).
// Adds extra "matchers" (assertion helpers) like expect(el).toBeInTheDocument().
import '@testing-library/jest-dom';
